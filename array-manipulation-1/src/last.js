/* exported last */
/*
* Define a function, named last, with one parameter, array
*    Return the last item of the array out of the function
*/

function last(array) {
  return array[array.length - 1];
}
