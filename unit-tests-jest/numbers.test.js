import { evenNumbers, toDollars, divideBy, multiplyBy } from './numbers';

describe(evenNumbers(), () => {

  it('');
});

describe(toDollars(), () => {

  it();
});

describe(divideBy(), () => {

  it();
});

describe(multiplyBy(), () => {

  it();
});
