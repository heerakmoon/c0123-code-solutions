/* exported setValue */
/*
* Define a function, setValue, with three parameters, object, key, and value
*   Assign value to the value of key from object
*/

function setValue(object, key, value) {
  object[key] = value;
}
